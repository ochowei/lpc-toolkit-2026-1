interface:
  display_name: "LPC Character Authoring"
  short_description: "Compose characters from existing LPC art"
  default_prompt: "Build an LPC character from existing catalog art and show me an attributed preview."
policy:
  allow_implicit_invocation: true
