{
  "name": "lpc-toolkit",
  "private": true,
  "version": "0.0.0",
  "description": "Monorepo: shared core for composing LPC character sprites + React web UI + CLI",
  "license": "GPL-3.0-or-later",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  },
  "packageManager": "pnpm@9.0.0"
}
